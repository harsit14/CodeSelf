experiment:
  name: rollout_smoke_example
  seed: 20260601

data:
  tasks: configs/datasets/tasks.example.jsonl
  split: train
  limit: 10

prompt:
  template: direct_solution_v1

generation:
  backend: mock
  samples_per_task: 4
  max_new_tokens: 512
  temperature: 0.8
  top_p: 0.95

execution:
  include_hidden: true

output:
  rollouts: outputs/rollouts/rollout_smoke.jsonl
